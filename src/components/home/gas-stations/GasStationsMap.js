import { View } from "react-native";

export default function Map() {
  return (
    <View style={{ flex: 1, height: '100%', backgroundColor: '#f00', alignItems: "center", justifyContent: "center" }}>
    </View>
  );
}
