import { Icon } from "@rneui/themed";

export default function AwaitingIcon({ size, color }) {
  return (
    <Icon
      name="calendar-clock"
      type="material-community"
      color={color}
      size={size}
    />
  );
}
